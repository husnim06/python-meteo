# Метеостанция на Arduino с Python бэкендом и Telegram ботом

Полноценная IoT система мониторинга погоды, которая собирает данные о температуре и влажности с Arduino, сохраняет в базу данных и предоставляет доступ через REST API и Telegram бота.

## 🌟 Возможности

- **Сбор данных в реальном времени** с датчиков DHT11/DHT22 через Arduino
- **REST API** на FastAPI для доступа к данным
- **База данных SQLite** с SQLAlchemy ORM
- **Telegram бот** для мгновенных уведомлений о погоде
- **Микросервисная архитектура** для масштабируемости

## 🛠 Аппаратные требования

- Arduino Uno/Nano
- Датчик температуры и влажности DHT11 или DHT22
- USB кабель
- Макетная плата и провода

## 📋 Программные требования

- Python 3.8+
- Arduino IDE
- Необходимые Python пакеты (см. requirements.txt)

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/husnim06/python-meteo.git
cd python-meteo
```

### 2. Установите Python зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте переменные окружения

```bash
cp .env.example .env
# Отредактируйте .env с вашими настройками
```

### 4. Загрузите скетч на Arduino

- Откройте `arduino/weather_station.ino` в Arduino IDE
- Установите библиотеку DHT sensor через Менеджер библиотек
- Выберите вашу плату и порт
- Загрузите скетч

### 5. Настройте приложение

Отредактируйте файл `.env`:

```env
ARDUINO_PORT=/dev/ttyUSB0  # Linux/Mac или COM3 для Windows
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=sqlite:///./weather.db
```

## 🎯 Использование

### Запустите сервисы (в отдельных терминалах):

**Терминал 1 - API сервер:**

```bash
python api/main.py
```

**Терминал 2 - Сборщик данных:**

```bash
python collector/main.py
```

**Терминал 3 - Telegram бот:**

```bash
python bot/main.py
```

### API эндпоинты

- `GET /` - Статус API
- `GET /api/current` - Текущие данные о погоде

### Команды Telegram бота

- `/start` - Приветственное сообщение
- `/current` - Получить текущую температуру и влажность
- `/help` - Получить справку по командам

## 🏗 Структура проекта

```
python-meteo/
├── api
│   ├── __init__.py
│   └── main.py
├── arduino
│   └── weather_station.ino
├── bot
│   ├── __init__.py
│   └── main.py
├── collector
│   ├── __init__.py
│   └── main.py
├── LICENSE
├── README.md
├── shared
│   ├── database.py
│   ├── __init__.py
│   ├── models.py
└── requirements.txt
```

## 🔧 Конфигурация

### Подключение Arduino

Обновите `ARDUINO_PORT` в `.env`:

- Windows: `COM3`, `COM4`, etc.
- Linux: `/dev/ttyUSB0`, `/dev/ttyACM0`
- Mac: `/dev/tty.usbmodem*`

### Telegram бот

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Добавьте токен в `BOT_TOKEN` в `.env`

## 📈 Планы по развитию

- [ ] Веб-интерфейс с графиками
- [ ] Уведомления при достижении пороговых значений
- [ ] Поддержка нескольких датчиков
- [ ] Экспорт данных в CSV
- [ ] Docker контейнеризация
- [ ] Авторизация для API

## 🤝 Как внести вклад

1. Сделайте форк проекта
2. Создайте ветку для функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект лицензирован under the MIT License - смотрите файл [LICENSE](LICENSE) для деталей.

## 🙏 Благодарности

- Команде FastAPI за отличный веб-фреймворк
- Разработчикам библиотеки Python-Telegram-Bot
- Сообществу Arduino за бесконечное вдохновение
