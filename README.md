# Python-meteo

Профессиональная система мониторинга температуры и влажности с веб-интерфейсом и Telegram-ботом.

## 🚀 Функциональность

- **Data Collection** - сбор данных с датчика DHT11 через Arduino
- **REST API** - FastAPI для доступа к историческим и текущим данным
- **Telegram Bot** - мгновенные уведомления о погодных условиях
- **Database** - надежное хранение метеорологических данных в SQLite
- **Docker** - быстрое развертывание проекта при помощи контейнеров
- **Анализ данных** - вывод статистики и графиков

## 🛠 Технологии

- **Backend**: FastAPI, SQLAlchemy, Uvicorn
- **Database**: SQLite
- **Hardware**: Arduino Uno + DHT11
- **Messaging**: python-telegram-bot
- **Serial Communication**: pyserial
- **Docker**: docker, docker-compose
- **Data**: matplotlib, pandas, numpy

## 🏗 Архитектура

```
python-meteo
├── api
│   ├── Dockerfile
│   ├── __init__.py
│   └── main.py
├── arduino
│   └── weather_station.ino
├── bot
│   ├── Dockerfile
│   ├── __init__.py
│   └── main.py
├── collector
│   ├── Dockerfile
│   ├── __init__.py
│   └── main.py
├── docker-compose.yml
├── LICENSE
├── README.md
├── requirements.txt
└── shared
    ├── database.py
    ├── __init__.py
    └── models.py
```

## ⚙️ Установка и настройка

### 1. Клонирование и настройка окружения

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корневой директории, укажите в нем свои параметры:

```bash
cp .env.example .env
```

### 3. Прошивка Arduino

1. Откройте `arduino/weather_station.ino` в Arduino IDE
2. Подключите Arduino к компьютеру
3. Выберите правильный порт и плату (Arduino Uno)
4. Загрузите скетч

## 🚀 Запуск системы

Запускайте сервисы в отдельных терминалах в указанном порядке:

### 1. Веб-API

```bash
python api/main.py
```

**Доступ:** http://localhost:8000
**Документация API:** http://localhost:8000/docs

### 2. Сервис сбора данных

```bash
python collector/main.py
```

**Назначение:** Читает данные с Arduino и сохраняет в базу данных.

### 3. Telegram бот

```bash
python bot/main.py
```

#### Либо запустите всё с помощью docker-compose одной командой

```bash
$ docker-compose up -d
# Из-за healthcheck может долго запускаться
```

#### Что-бы остановить контейнеры docker-compose выполните следующую команду

```bash
$ docker-compose down
```

## 📡 API Endpoints

| Метод | Endpoint       | Описание                                                      |
| ----- | -------------- | ------------------------------------------------------------- |
| `GET` | `/api/current` | Текущие показания температуры и влажности                     |
| `GET` | `/api/history` | Отображение истории температуры и влажности за период         |
| `GET` | `/api/chart`   | Создание и вывод графика температур и/или влажности за период |
| `GET` | `/docs`        | Интерактивная документация Swagger                            |
| `GET` | `/health`      | Проверка состояния                                            |

**Пример запроса:**

```bash
curl http://localhost:8000/api/current
```

**Пример ответа:**

```json
{
  "temperature": 23.5,
  "humidity": 45.2,
  "timestamp": "2024-01-15T14:30:00Z"
}
```

## 🤖 Telegram Bot Commands

- `/start` - начать работу с ботом
- `/current` - получить текущие погодные данные
- `/stats [часы]` - статистика за период (по умолчанию 24 часа)
- `/chart [часы] [temp/hum]` - график температуры/влажности
- `/help` - справка по командам

## 🔧 Troubleshooting

### Проблемы с последовательным портом

**Определение правильного порта:**

- **Windows:** Диспетчер устройств → Порты (COM и LPT)
- **Linux:** `ls /dev/tty*`
- **Mac:** `ls /dev/tty.*`

### Проблемы с датчиком DHT11

1. **Проверьте подключение:** VCC → 5V, DATA → Pin 2, GND → GND
2. **Добавьте pull-up резистор:** 10кОм между DATA и VCC
3. **Проверьте питание:** DHT11 требует стабильного 5V

### Проблемы с базой данных

```bash
# Проверьте создание базы данных
sqlite3 weather.db ".tables"
```

## 📊 Валидация данных

Система автоматически проверяет корректность данных:

- Температура: от -50°C до +60°C
- Влажность: от 0% до 100%

## 🔄 Логирование

Все сервисы ведут детальное логирование:

- Data Collector: сбор данных с Arduino
- API: запросы к базе данных
- Bot: взаимодействия с пользователями
